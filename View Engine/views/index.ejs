<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Motivational Day</title>
    <link
      href="https://fonts.googleapis.com/css?family=Montserrat:700,400&display=swap"
      rel="stylesheet"
    />
    <style>
      body {
        background: linear-gradient(135deg, #6dd5ed 0%, #2193b0 100%);
        min-height: 100vh;
        margin: 0;
        font-family: "Montserrat", sans-serif;
        display: flex;
        align-items: center;
        justify-content: center;
      }
      .container {
        background: rgba(255, 255, 255, 0.95);
        border-radius: 24px;
        box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.2);
        padding: 48px 40px;
        max-width: 420px;
        text-align: center;
      }
      .day {
        font-size: 2.5rem;
        font-weight: 700;
        color: #2193b0;
        margin-bottom: 16px;
        letter-spacing: 2px;
      }
      .message {
        font-size: 1.3rem;
        color: #333;
        margin-bottom: 32px;
      }
      .work-hard {
        font-size: 1.1rem;
        color: #fff;
        background: linear-gradient(90deg, #43cea2 0%, #185a9d 100%);
        padding: 14px 24px;
        border-radius: 12px;
        display: inline-block;
        font-weight: 600;
        box-shadow: 0 4px 16px rgba(67, 206, 162, 0.15);
        letter-spacing: 1px;
      }
      @media (max-width: 600px) {
        .container {
          padding: 24px 10px;
        }
        .day {
          font-size: 2rem;
        }
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="day">Today is <%= day %>!</div>
      <div class="message">
        Remember, every day is a new opportunity.<br />
        <span style="color: #185a9d; font-weight: 600"
          >Make the most of <%= day %>!</span
        >
      </div>
      <div class="work-hard">
        You have to work very hard today.<br />
        Success is built on effort!
      </div>
    </div>
  </body>
</html>
